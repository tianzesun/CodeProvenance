# GPU Server Requirements for CodeBERT/UniXcoder
# Install: pip install -r requirements-gpu.txt

# Core ML
transformers>=4.30
torch>=2.0
accelerate>=0.20

# Queue (production GPU worker)
redis>=4.0
celery>=5.0

# Batch processing
numpy>=1.24

# Monitoring
psutil>=5.0
