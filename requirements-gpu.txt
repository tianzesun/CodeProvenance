# GPU Server Requirements for CodeBERT/UniXcoder
# Install on GPU servers only: pip install -r requirements-gpu.txt

# Core ML / GPU Acceleration
transformers==4.41.2
torch>=2.0
accelerate>=0.28
optuna==3.6.1
huggingface-hub==0.25.1

# Included dependencies are already covered in base requirements.txt
# Install base requirements first, then this file for GPU support
