import React from "react";

/**
 * SideBySideDiff - Displays Student A vs Student B with highlighted match regions.
 * DEPRECATED: Use DualHeatmap.js instead
 */
export default function SideBySideDiff({ data, onSelectRegion }) {
  return (
    <div className="bg-white p-4 text-center text-gray-400">
      Visual Heatmap Viewer active
    </div>
  );
}